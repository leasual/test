
#include_directories(${CMAKE_CURRENT_SOURCE_DIR})
#
#add_library(tvm_runner STATIC tvm_runner.cc tvm_data.cc)
#target_link_libraries(tvm_runner dmlc tvm_runtime ${LIBRARIES})

include_directories(${CMAKE_CURRENT_LIST_DIR})
